;calculator.vspl by DiamondCoder1000
#include "/lib/
function calculator(){
  print("calculator");
  println();
  print("type 'exit' to exit");
  while (true) {
    print("first num:");
    a = input_string();
    if (a == "exit"){
      return();
    }
    copy(A, B);
    print("second num:);
    b = input_string();
    add(A, B);
    print(B);
  }
}
